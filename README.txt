[ID] boost_ratio isi sama boost kalian jika tidak ada isikan 0 saja, authorization dan referer ambil dari link user/stast bagian request, av, v dan bid ambil di url, sdk sampai x_state ambil di url android/stast, jika php bot.php sudah dijalankan config akan ada 2 [1] config.php, [2] config.json, kalau mau edit config edit yg config.php, config.json itu untuk backup config sc ini bisa di cloning, bagian x_state jika tidak ada isikan null saja jika sc sudah jalan beberapa jam x_state akan terisi sendiri

[EN] boost_ratio fill with your boost if there is no 0, authorization and referrer take from the user / stast link request, av, v and bid take in url, not until x_state takes in url android/stast, if php bot.php has been run  config there will be 2 [1] config.php, [2] config.json, if you want to edit config edit config.php, config.json for backup config this script can be cloned, x_state part if there is no fill in null if script  have been running a few hours x_state will fill itself